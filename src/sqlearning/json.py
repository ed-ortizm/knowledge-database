"""Utilities to interact with json files"""

def save_json_data():
    """Save the filled JSON data to a file"""

    pass


    # json_data = {
    #     "insights": {
    #         "summary": {
    #             "summary": summary_var.get(),
    #             "strong_points": [strong_points_var.get()],
    #             "weak_points": [weak_points_var.get()]
    #         },
    #         "takeaways": takeaways_var.get().split('\n'),
    #         "ideas": ideas_var.get().split('\n'),
    #         "reading_stage": reading_stage_var.get()
    #     },
    #     "material_tags": {
    #         "tags": tags_var.get().split('\n')
    #     },
    #     "reading_material": {
    #         "title": title_var.get(),
    #         "material_type": material_type_var.get(),
    #         "publisher_link": publisher_link_var.get(),
    #         "publisher_identifier": publisher_identifier_var.get(),
    #         "short_summary": short_summary_var.get(),
    #         "external_summary_path": external_summary_path_var.get(),
    #         "created_at": created_at_var.get(),
    #         "updated_at": updated_at_var.get()
    #     },
    #     "publishers": {
    #         "name": publisher_name_var.get()
    #     }
    # }

    # # Save the JSON data to a file (you can customize the filename)
    # with open('filled_data.json', 'w') as json_file:
    #     json.dump(json_data, json_file, indent=4)